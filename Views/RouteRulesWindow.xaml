<Window
    x:Class="Orayo.Views.RouteRulesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Orayo.Helpers"
    mc:Ignorable="d"
    Title="{local:Localize Key=TitleRoute}">

    <Grid Padding="24,8,20,20" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBarGrid" Margin="-24,-8,-20,0" Padding="24,12,20,12" Background="Transparent">
            <TextBlock x:Name="TitleBarTextBlock" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
        </Grid>

        <StackPanel Grid.Row="1" Spacing="4">
            <StackPanel Orientation="Horizontal" Spacing="4">
                <TextBlock Text="{local:Localize Key=ConfigHint}" Opacity="0.72" VerticalAlignment="Center" />
                <HyperlinkButton Content="Project X - Routing"
                                 NavigateUri="https://xtls.github.io/config/routing.html"
                                 Padding="0"
                                 MinWidth="0"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Center" />
            </StackPanel>

            <TextBlock x:Name="ErrorTextBlock"
                       Foreground="IndianRed"
                       TextWrapping="Wrap"
                       Visibility="Collapsed" />
        </StackPanel>

        <WebView2 x:Name="EditorWebView" Grid.Row="2" />

        <Grid Grid.Row="3" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="{local:Localize Key=ButtonFormat}" MinWidth="88" Click="FormatButton_Click" />
                <Button Content="{local:Localize Key=ButtonReset}" MinWidth="88" Click="RestoreDefaultsButton_Click" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Style="{StaticResource AccentButtonStyle}" Content="{local:Localize Key=ButtonSave}" MinWidth="96" Click="SaveButton_Click" />
                <Button Content="{local:Localize Key=ButtonCancel}" MinWidth="96" Click="CancelButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>


